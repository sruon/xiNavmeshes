# Navmeshes — LSB branch `base`

Baseline meshes, generated by unmodified base code (no off-mesh links, coarse stepping).

- Source: LandSandBoat/server branch `base` @ a7d4c3c05d
- Config: base defaults (cellHeight 0.4-equivalent, agentMaxClimb 0.6, no off-mesh links)
- Zones: 305

Compare against branch `navmesh_offmesh_links` in the xi-visualizer Navmesh Diff page.
